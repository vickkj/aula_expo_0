import { StyleSheet } from "react-native-web";

const styles = StyleSheet.create({
    
    mensagem: {
        fontSize: 25,
        margin: 10,
        color: 'red',
    },
    viewmensagem: {
        borderWidth: 2,
        borderColor: 'Red',
        borderRadius: 20,
    }
});

export default styles;